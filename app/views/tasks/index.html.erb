<% @tasks.each do |task| %>
<h1><%= link_to task.title, task_path(task) %></h2>
<p><%=  task.details %></p>
<p><%= if task.completed == true
          "[x] tache done"
          else
          "[ ] tache done"
        end
%></p>
<% end %>
